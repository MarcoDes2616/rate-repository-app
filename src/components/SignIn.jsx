import React from 'react';
import { View, Text } from 'react-native-web';

const SignIn = () => {
    return (
        <View>
            <Text>Sign in</Text>
        </View>
    );
};

export default SignIn;